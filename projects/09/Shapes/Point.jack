class Point {
    field int x,y;

    constructor Point new(int Px, int Py) {
        let x = Px;
        let y = Py;
        return this;
    }

    method void dispose() {
        do Memory.deAlloc(this);
        return;
    }
}
